# Configliere

Configliere is being rebuilt from first principles.

The public API is intentionally empty while the new design is established
through executable sketches and tests. The previous implementation remains
available in Git history at commit `1673782`.
